# analysis/steps/full.py
#
# Ce fichier était un stub vide enregistré dans le registry sous le nom "full".
# Il a été retiré du registry car il ne fait rien et peut masquer des erreurs
# (un step silencieusement no-op dans un pipeline est difficile à déboguer).
#
# Si un mode "full" est nécessaire, définir la séquence explicitement dans
# analysis/core/run.py sous la clé "full" du dispatch, comme c'est déjà
# le cas — voir run_analysis(mode="full").
#
# NE PAS @register_step("full") ici.